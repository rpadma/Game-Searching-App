<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.etuloser.padma.rohit.homework5">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameDetailsActivity"
            android:label="@string/gamedetailslbl" />
        <activity
            android:name=".WebViewActivity"
            android:label="@string/WebViewlbl" />
        <activity android:name=".SimilarGamesActivity"
            android:label="@string/similatgamelbl"

            ></activity>
    </application>

</manifest>